import DaumPostcode from 'react-daum-postcode';

/**
 * 카카오 주소 API 팝업
 * @returns
 */
const AddressPopup = () => {
  return (
    <div>
      <DaumPostcode />
    </div>
  );
}

export default AddressPopup;